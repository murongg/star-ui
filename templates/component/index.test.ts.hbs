import { mount } from '@vue/test-utils'
import mountTest from '../../../../tests/share/mountTest'
import {{firstLetterToUpperCase name}} from '../src/index'

describe("{{firstLetterToUpperCase name}} Component", () => {
  mountTest({{firstLetterToUpperCase name}})
})
