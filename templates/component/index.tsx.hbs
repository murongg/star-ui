import { defineComponent, ComponentCustomProps, SetupContext } from "vue";

export interface I{{firstLetterToUpperCase name}}Props extends ComponentCustomProps {}

export default defineComponent({
name: 'star-{{name}}',
props: {
},
setup(props) {
}
})